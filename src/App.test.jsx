import { render } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'
import { UserProvider } from './context/UserContext'
import App from './App'

describe('App.jsx tests', () => {
  it('should test a user signing in', async () => {
    render()
  })
})